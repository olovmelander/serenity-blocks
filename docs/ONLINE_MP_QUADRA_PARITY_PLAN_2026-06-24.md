# Online MP — Quadra-Parity Best-in-Class Plan (2026-06-24)

Source: 27-agent adversarially-verified comparison of **Quadra** (`C:/Users/olovm/repositories/quadra`, C++ deterministic-lockstep) vs **Serenity Blocks** (host-authoritative + prediction). Every claim below was re-read and confirmed against cited code in both repos.

## Implementation status (2026-06-24)

- **⭐ PEER-OWNS-BOARD (Quadra local-sim) — SHIPPED (`?peerLocalSim`, default on).** The architectural fix for "feel exactly like local/host": the incremental hold/reconcile patches plateaued because the peer re-based its own board against the host's stale 30Hz snapshot every frame. Determinism is verified feasible (shared-seed integer-LCG piece stream identical on both sides; clears/cascades integer-pure; hard-drops bit-identical per the demo-replay test). So the peer now FULLY OWNS its grid/piece/nextPieces/score/lines/level as a local sim — never re-based per-frame; the host snapshot supplies only incoming garbage + frags/death (verdicts) + a **caught-up+settled** desync backstop (compare to host only once it has applied all our inputs and both are settled → one rate-limited resync on a true divergence). The local board then renders like LocalMP (direct from gameState, instant input). Only non-bit-identical source is gravity-lock timing (both sides RAF wall-clock, not fixed-step) → rare divergence → resync; true bit-identical needs the fixed-step clock (Phase 4/5). Unit `ffa-peer-local-sim.test.js` 4/4; suite 521/522; harness @80-110ms × 34 drops → **0 resyncs, 0 score regressions, 0 errors**. Needs real 2-machine validation of resync frequency; `?peerLocalSim=0` reverts.


- **Phase 0 — SHIPPED & verified.** score/lines/level + garbage held with the grid during the local predicted clear (monotonic `Math.max`), Signal 4 (`isProcessingPhysics`) holds across the whole cascade, round-gen reset. Behind `?holdStats`. Harness: score-regression frames 68→1, `desyncCount` 0, round-2 clean, suite 508/509. Fixes the peer's OWN clears/cascades/combos feeling off.
- **Phase 1+2 — SHIPPED & verified.** Opponent clears now animate: host broadcasts a grid-free `GAME_LINES_CLEAR` per cascade (new `onLineClear` callback + `player._clearSeq`); peer `_applyPlayerClear` re-emits a dedicated `OPPONENT_CLEAR` event; OnlineMultiplayerMode routes it to a per-opponent `CanvasBoardEffects` overlay (row-flash + combo badge) on a separate z-12 canvas that never writes the grid (so it can't refight the snapshotInterpolator). Behind `?opponentClearEvents`. Harness: full chain host→peer verified, screenshot confirms the row-flash + "4× Combo!" on the opponent mini-board, opponent grid stays live (no interp fight), suite 508/509. Raw-binary transport (Phase 1 item 3) deferred as risky/non-essential.
- **Garbage "strange on peer" — FIXED & verified.** Non-idempotent double-source: peer predict-consumes a burst instantly while the host consumes ~½RTT later, so the snapshot re-adds it → double rows + meter rebound (Phase 0's queue-freeze widened the window). Fix = idempotent adoption keyed on `attackId:lineIndex` (peer records predict-consumed bursts, filters them out of the adopted host queue until the host stops listing them); removed the Phase 0 queue-freeze (filter supersedes it). Behind `?garbageIdempotent`. Unit test `ffa-garbage-idempotent.test.js` 5/5; suite 513/514; harness clean.
- **Smoothness/perf (toward local-MP feel) — SHIPPED.** Re-instated the OpponentWatchManager dirty-check (repaint a mini-board only on grid/piece/blind change instead of unconditional 60–144fps flood-fill; ~90% steady-state opp CPU cut; `?opponentDirtyCheck`), and dirty-guarded the per-frame local player-card style block (only on color/size change). Harness: all live, console clean.
- **Garbage DRAIN-ALL (match local) — SHIPPED.** Local drains the whole garbage queue per spawn (one dump); online trickled one burst per spawn. New `_drainAllLineBursts` helper; host `insertPendingGarbage` + peer `_insertLocalGarbagePrediction` now drain the whole queue per spawn (both behind `?garbageDrainAll`, default on; idempotent dedup records every drained line). Also reduces cascade-chain/combo divergence (garbage consumed at the same logical point on both sides). Unit tests 6/6; suite 514/515.
- **Peer "stack gets higher for a second then resets" on cascade/clear — FIXED (Signal 5).** After the peer's predicted cascade clears lines, the host (~RTT behind) acks the lock input but its async cascade hasn't finished, so its snapshot still has the taller pre-clear grid — the hold released and the peer adopted it ("jumps higher") then reset when the host caught up. Fix: hold the predicted cleared grid while `host lines < peer lines` (`lines` is the monotonic "host finished the clear" signal); also guard `localAhead` so a host-cleared-more frame doesn't mis-trip it. Unit test `ffa-cascade-hold.test.js` 3/3; suite 517/518.
- **⭐ Three visual-polish fixes (after "It feels great now!") — SHIPPED & harness-verified.** Make online MP *look* like local too. (1) **Theme un-frozen during MP:** `OnlineMultiplayerMode._suspendThemeForMatch()` flipped opt-OUT→**opt-IN** — suspends only when `localStorage 'serenity.mpSuspendTheme'==='1'`; default keeps the theme animating like single-player (the A5 GPU-contention relief is now explicit opt-in). Verified: in-match `themesSuspended:false`, screenshot shows the live summer scene behind the boards. (2) **Garbage rows in attacker color:** serialize/adopt/render were already color-aware; the binary codec was the only gap. Added a **3-byte RGB field** to `_encodeGarbageEntry`/`_decodeGarbageEntry` (`GARBAGE_ENTRY_FIXED_BYTES_V5=27`, `FORMAT_VERSION` 4→5, version-gated decode; `#000000`⇒undefined→grey fallback). Render side unchanged. Verified: extended `binary-encoding-roundtrip.test.js` + live in-app `import()` of the codec singletons round-trips `['#ff1744','#2979ff',null]`. Limitation: garbage already settled into a remote opponent's *streamed grid* stays grey (4-bit grid codec has no per-cell color). (3) **Opponent mini-board pieces = active theme:** OWM's persistent `_colorCache` shadowed the per-board style-manager's THEME_CHANGED refresh. Fix: OWM subscribes THEME_CHANGED + `settingsChanged` → clears `_colorCache`+`_renderSigs`+`styleManager.refresh()`; also clears cache after lazy `styleManager.init()`; unsubscribes in `destroy()`. Verified: screenshot — opponent O-piece matches the host main board's gold (same palette across all boards). Suite **522/523** (same 1 unrelated Odyssey fail). Final garbage-color/theme look = user's real-play confirm.
- **⭐ Transactional logs (Battle Log + lobby Activity Log) — SHIPPED & harness-verified.** User: the in-match Battle Log "feels like it resets" and the lobby Activity Log resets + isn't shared host↔peers. 35-agent verified workflow found the events ALREADY reach host+peers (garbage = host local echo + broadcast; death = PLAYER_TOPPED_OUT echo + broadcast; eventId-deduped), so the bug was PERSISTENCE, fixed surgically (not the riskier host-authoritative shared-log rewrite the agents proposed). **Battle Log (OnlineKillFeed):** was a transient HUD — `itemTTL=12000` (rows auto-expired after 12s), `maxItems=12`, and `clear()` on ROUND_RESTART. Now: constructor `options` default `itemTTL=Infinity` (never expire; `_scheduleExpire` no-ops on non-finite TTL) + `maxItems=200` (panel already scrolls); per-round clear replaced with an `addRoundMarker()` "— Round N —" divider. **Activity Log (lobby-waiting-room.js):** local-derived roster diffs with a silent first-seed → joining peer saw an empty log; `_finalizeRemovePlayer` never emitted `PLAYER_LIST_CHANGED` locally → host never logged leaves. Now: first-seed logs each present member; `_finalizeRemovePlayer` emits `PLAYER_LIST_CHANGED`. Verified: both nodes show identical complete activity logs (incl. a captured ready toggle); battle-log entries persisted past 14s + a forced render on both; unit test `online-battle-log.test.js` 7/7; suite 529/530. Out-of-scope (flagged): FragTracker's separate 10-cap killFeed still `reset()`s per round (results modal shows last round only); no full-log resync for late joiners; lastAttackerId not in the binary codec.
- **Cascade/clear/chain/combo vs local — verified divergences (10-agent compare):** chain length can diverge under attacks-near-a-lock (garbage on two clocks); combo number is predict-only (never reconciled); score is `Math.max`-pinned so over-predictions never correct down; attack scaling/cancellation differ from local. Drain-all addresses the garbage feel + reduces chain divergence; the rest are the next increments below.
- **Deferred (need 2-machine validation / touch shared physics / gameplay-balance):** COMBO-number reconcile (`?comboReconcile` — host already sends authoritative cascadeCount; surface it as a `COMBO_CONFIRM` and overwrite the popup only when it differs, matched on clearSeq), SCORE lockSeq-reconcile (hard-set per-lock score at `onCascadeComplete` instead of `Math.max`), cascade garbage-apply-ORDER (`?garbageApplyOrder` — gate peer consume on `applyAfterLockSeq`) + host-gated hold release, ATTACK scaling/cancellation parity (unify into one shared helper using the LOCAL table), Phase 1 reconcile, Phase 3 ready-barrier/keyframe-coupling, Phase 4 (frame-lock to simTick, seed-as-authority bag, replay harness). (`registerAttackerIds` is no longer needed for garbage TINT — color is now encoded explicitly in codec v5 — it only affects decoded `attackerId` for frag attribution.)

## Executive summary

Serenity's online MP is **host-authoritative full-board snapshot streaming + client prediction + reconciliation**. Quadra is **peer-deterministic lockstep input-streaming** (shared seed → every machine regenerates the identical piece stream and runs one frame-stepped sim; the local sim is never overwritten; the lock is the only authoritative discrete event; clears/cascades/combos are the deterministic *consequence* of the stamped lock, frame-identical everywhere, free on the wire).

The two shipped fixes (LOCAL-PIECE OWNERSHIP + LOCAL-BOARD HOLD) made **move/rotate/drop** smooth on the peer because those are instantaneous, single-cell, locally-owned. **Line clears / cascades / combos still feel "way off"** because they are derived, multi-frame, async-animated events that are effectively **triple-owned**: (a) the peer runs its own async `processPhysics` animation on its RAF clock; (b) the host runs *its* own async `processPhysics` on a different clock and broadcasts the settled result; (c) the LOCAL-BOARD HOLD freezes only the **grid** during the post-lock window while **score/lines/level** (`ffa-p2p-game-state.js:2652-2654`) and the **garbage queue** (`2774-2796`) are adopted from the host **every frame, outside the hold gate**.

Visible result on the peer: the score/lines counter **regresses to the host's pre-clear total then jumps**; the flash/combo/cascade-wave fire on the local-prediction beat while the board settles on a different beat; the garbage meter **churns** (predicted-down → snapshot-up → host-consumes → snapshot-down); and a deep cascade that outlasts the 250ms window / 30-frame cap releases the grid hold **mid-animation** onto a host frame at a different cascade stage. Amplifier: while `isProcessingPhysics` is true the peer also buffers its own inputs (`1222`), so during a cascade the whole board+prediction is frozen yet only stats/garbage move.

## Architectural verdict

**KEEP host-authoritative + prediction and perfect it via a Quadra-inspired hybrid. Do NOT migrate to full deterministic lockstep.**

Why not lockstep: Serenity is structurally hostile to bit-determinism today — float physics (not fixed-point), RAF-paced variable-delta sim (not a fixed quantum), clears/cascades are async wall-clock animations (not deterministic ticks), and the bag is host-pushed (not seed-derived). Converting all four is an XL, high-risk rewrite of the **shared single-player core** and throws away the working prediction/ownership/hold investment. Quadra can *assert* determinism (validate the stamp, drop on mismatch) because C++ integer math guarantees it; JS float under async timing cannot, so a half-migrated lockstep would desync silently — strictly worse.

Quadra's smoothness comes from **three separable mechanisms Serenity can adopt without bit-determinism**:
1. **The clear is owned by ONE timeline and never overwritten mid-animation** → make the predicted clear a single self-consistent window (Phase 0).
2. **The lock is a discrete RELIABLE EVENT carrying the authoritative result** → extend `_emitAuthoritativeLock` to carry clear semantics (Phase 1).
3. **Opponents REPRODUCE the clear animation** instead of teleporting between settled grids (Phase 2).

The host-authoritative safety net (chunked resync) stays as the divergence backstop. The seed/lockSeq/simTick plumbing added here keeps a future 1v1-only lockstep experiment possible.

## Quadra's secret sauce (verified)

- **Deterministic per-canvas RNG from one shared seed** (`canvas.cc:43/83 rnd(game_seed)`, `random.cc:38` LCG `seed*0x41c64e6d+0x3039`, `player.cc:269 the_next=rnd()%7`) → identical piece stream everywhere, no queue transmitted.
- **Fixed-timestep deterministic tick**: one 10ms accumulator (`quadra.cc:425-432`) advances a global `overmind.framecount`; every canvas stepped once per tick (`net_list.cc:412-417`). Sim decoupled from render; clears run *inside* the stepped sim.
- **Fixed-point integer physics** (`canvas.cc:228-245`, `<<4`) → bit-identical, nothing to reconcile.
- **Inputs (not state) streamed reliable in-order** (TCP, Nagle off): one bit-packed byte/frame (`player.cc:451-460`), flushed at 50 bytes or on lock. Board grid never transmitted during play.
- **Remote players REPRODUCED, not sampled**: `Player_wait_block` replays one input byte/frame through the *same* move/rotate/drop functions (`player.cc:1308-1357`) → full frame-rate motion, nothing to interpolate.
- **The lock is the single authoritative discrete event** (`Packet_clientstampblock`, `player.cc:478-499`), reliable/ordered/idempotent, force-flushes pending moves. The clear is the deterministic *consequence* (`Player_stamp→check_line→flash_lines→check_link`, `player.cc:528-833`).
- **Single writer**: the sim mutates the same arrays the renderer reads (`zone.cc:64-73`). No interpolator, no two-writer fight.
- **Garbage crosses as an explicit `Packet_lines` event** with exact hole positions (`net_list.cc:122-142`), applied to local canvases — never reconciled state.
- **Round transitions = one-byte all-ready barrier + one shared reseed** (`net_list.cc:826-870`, `game.cc:941-951`); no board payload crosses a round boundary.
- **Determinism is verifiable**: `recording.cc` replays seed+inputs bit-identically; `Player_stamp` validates the streamed pose vs the locally-reproduced board (desync → server drop).

## Phase 0 — Ship-now: make the predicted clear a single self-consistent window

**Goal:** kill the loudest peer-clear artifacts (score/lines regress-then-jump, garbage-meter churn, mid-cascade grid twitch). Lowest risk, highest perceived-quality return. Behind `?holdStats` (default on with `?localBoardHold`).

- **(A)** Gate **score/lines/level** adoption (`2652-2654`) under `ownsLocalPiece && holdLocalBoard`: while holding, keep locally-predicted values; adopt host stats **atomically on hold release**.
- **(B)** Guard the **garbageQueue** wholesale-replace (`2774-2796`) with `&& !(ownsLocalPiece && holdLocalBoard)` for the local player; host POST-consumption queue wins atomically on release.
- **(C)** Add hold **Signal 4** `localProcessingPhysics = player.gameState.isProcessingPhysics === true`; do not count `isProcessingPhysics` frames toward the 30-frame cap — use a separate **90-frame** safety cap. Ties the hold to the actual cascade animation.
- **(D)** **Reset hold/prediction state on round restart** (`restartMatch`, `performRoundRestart`): zero `_localBoardHoldCount`, `_lastLocalLockTime`, `inputSequence`, per-player `lastInputSeq`; stamp `_localBoardHoldRoundGen=roundGeneration` and break the hold across a gen boundary.
- **(E)** Extend the **hold-aware desync gate** to cover the held-stats window so a mid-cascade hold-lapse cannot fire a false `_requestResync`.

**Risks:** held stats delay genuine corrections (mitigated by 90-frame cap + atomic adopt-on-release + flag); garbage gate must let host POST-consumption queue win on release; verify `_desyncCount` stays 0 across deep cascades.

**Verify (2-window harness @ 90ms+20ms jitter):** score/lines **monotonic** across single/Tetris/cascade/combo/perfect-clear; garbage meter moves **once**; flash/combo coincide with grid settle; deep cascade (>250ms) holds until `isProcessingPhysics` clears (no mid-fade twitch); on release digest matches host (`_desyncCount`=0); round-2 first clears don't spuriously hold; impairment-off unchanged.

## Phase 1 — Authoritative CLEAR-RESULT event (extend the lock channel)

Give clears/cascades/combos a discrete, reliable, ordered wire representation (Serenity's analogue of stampblock + deterministic clear).

- Extend `_emitAuthoritativeLock` (`3832-3851`) to carry `{playerSteamId, lockSeq, roundGeneration, hostTick, linesCleared[], holeMask, cascadeDepth, comboTier, b2b, perfectClear, tSpin, scoreDelta, garbageSent}` (reuse the summary already built in `physics.js` `onGarbageReady`).
- On the local peer, treat predicted `onLineClearImpact`/`triggerCombo`/score as **provisional**; reconcile to confirmed event values **at the animation boundary** (`onCascadeComplete`/hold release), keyed on `lockSeq`. Drive kill-feed/scoreboard from confirmed values.
- Cut hot-path waste: drop the `JSON.stringify(data).length` debug field (`steam-networking.js:490`); send the binary snapshot **raw** instead of base64+JSON wrap, side-channel fields in a small fixed binary header. Bump `protocolVersion`.

## Phase 2 — Opponent clears reproduce the staged animation

Make opponent clears/cascades/combos animate on the watcher instead of teleporting between settled grids.

- New opponent-facing clear path: on a Phase-1 clear-result event for a **non-local** steamId, look up the opponent board (`OpponentWatchManager`) and trigger a staged flash of `linesCleared` rows (~120ms) then let the next grid render the collapse. Add `_flashRows(board, rows, ms)`; new handlers that no longer early-return for opponents (reuse `triggerLineClearFlash`/`playLineClearImpact`/`showComboPopup`).
- Stage multi-cascade flashes sequentially using `cascadeDepth`/per-step rows.
- Re-instate dirty-checking in `OpponentWatchManager._animate` (`_computeBoardHash`/`_computePieceHash`) so the per-frame flood-fill only runs on change. **MUST route through the single opponent render path, not `renderAllPlayers`**, or it refights the interpolator (the historical lock-events regression).

## Phase 3 — Round-2 robustness & cadence (folds in known-open)

- Unify round-2 host spawn with match-start via one prepare/spawn helper (reset occupancy, clear active piece, fillBag, spawnPiece, assert no collision before `gamePhase='playing'`); jitter-buffer re-add after reset, before first spawn. **Keep the "do NOT `new GameState()`" constraint — reset in place.**
- Re-arm `startHeartbeatLoop()` on the round-2 path; gate host-migration election on a heartbeat guaranteed live across rounds (`host-migration.js:44`).
- Force an immediate reliable keyframe on the round-start tick (after `resetSnapshotBaselines`); event-couple keyframes to clearing locks (keyframe immediately after a clear/garbage lock).
- Optionally flip `_readyBarrierEnabled` default on (after Phase-0 resets land) — Serenity's analogue of Quadra's `syncto`.

## Phase 4 — Optional: frame-lock cascades & seed-as-authority

- Drive cascade advance off a fixed sim-tick step (existing `SIM_TICK`/`simTickNetcode` path); stamp each clear with the lock's `simTick` so host/peer cascades advance the same number of ticks and can phase-align.
- Stop host-pushing `nextPieces` in steady-state delta; adopt preview only on keyframe/resync; advance the bag once per acknowledged `lockSeq` on both sides (shared seed owns the preview).
- Deterministic-replay verification harness (record host input+lock+clear-event, replay against a fresh sim, assert convergence) under NetworkImpairment presets.

**Risk:** Phase 4 touches the shared single-player physics core (affects offline modes). Gate behind `simTickNetcode`; keep RAF path default until verified.

## Open questions

- Which path actually throttles broadcast cadence (`hasSignificantStateChanges` gate vs `broadcastGameState`/`startStateSyncLoop`)? (`_queueSnapshot` dropRate is already fixed — the "20Hz no-op" label is stale.)
- Should the clear-result event REPLACE the settled grid for the clearing player (cleaner, more coupling) or co-exist as a reconcile hint (safer, two writers)?
- After Phase 0, keep `_insertLocalGarbagePrediction` for instant local meter feedback (dual-sourced + reconcile) or suppress it (single-sourced)?
- Should `_readyBarrierEnabled` default-on wait until host-migration epoch fencing is also on (barrier timeout during migration could deadlock)?
