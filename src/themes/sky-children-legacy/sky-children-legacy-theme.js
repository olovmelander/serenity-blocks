/**
 * Sky Children Legacy Theme Alias
 *
 * Keeps the pre-v2 implementation addressable for rollback/testing while
 * `sky-children` points to the new v2 renderer.
 */

import SkyChildrenLegacyTheme from '../sky-children/sky-children-theme.js';

export default SkyChildrenLegacyTheme;
